import InputLabel from "@mui/material/InputLabel";
import {styled} from "@mui/material/styles";

const StyledTableNameLabel = styled(InputLabel)(({theme}) => ({
    fontSize:'25px',
    fontWeight:'500',
    color:'primary'
}));

export default StyledTableNameLabel;